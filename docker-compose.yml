version: '3.8'
services:
  db:
    image: postgres:15
    container_name: Spring-Identity-Core-db
    environment:
      POSTGRES_USER: ${USER_NAME}
      POSTGRES_PASSWORD: ${PASSWORD}
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"